- name: Install mariadb
  package:
    name: "{{ item }}"
    state: "present"
    disablerepo: "*"
    enablerepo: "yunion-*"
  with_items:
    - mariadb-server

- name: python pip
  pip:
    name: PyMySQL
